package com.example.main.routine

data class ExerciseData(
    val name: String,           // 운동 이름
    val bodyPart: String,       // 운동 부위 (가슴, 등, 하체 등)
    val bodyPartType: String    // 상체 / 하체 구분
)
